# 2A.4 — Multi-Landscape Evaluation

This stage evaluates acquisition transfer beyond the RhlA development landscape.

Frozen controllers:

- deterministic random sanity floor;
- historical 50/50 reference;
- Relative-Evidence V2 adaptive candidate.

Landscapes:

- GB1;
- TrpB;
- PhoQ.

Protocol:

- deterministic identity-only campaign pool: 2048;
- bootstrap: 64;
- final budget: 256;
- primary cadence: micro_8;
- sensitivity cadence: micro_16;
- no per-landscape retuning.

Outputs:

- per-condition discovery curves;
- controller pressure trajectories;
- `MULTILANDSCAPE_COMPARISON.csv`;
- `PAIRED_CONTROLLER_DELTAS.csv`;
- `MULTILANDSCAPE_MANIFEST.json`.

Read `PREREGISTRATION.md` before interpreting results.
