# NABU V7 — Higher-Order Generalization Gate

This experiment is frozen before execution.

It asks a narrow question: does the frozen full NABU consensus add hidden-combination design-selection value beyond the raw pairwise interaction arm?

Run:

```bash
python -m pip install pandas numpy scipy
python experiments/v7_higher_order/run_v7.py YOUR_DATASET.csv --out nabu_v7_results
```

Do not change the split, scoring rules, candidate budget, shuffle, or pass gate after viewing outcomes. A changed protocol must be V8 on fresh data.
